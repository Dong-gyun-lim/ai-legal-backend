<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.divorceai.mapper.AnalyzeMapper">

    <!-- 초기 버전: 나중에 DB 저장 단계에서 실제로 사용 -->
    <insert id="insertAnalyzeResult">
        INSERT INTO analyze_results (
            request_id, answer, similarity, damages, custody, created_at
        ) VALUES (
            #{requestId}, #{response.answer}, #{response.similarity},
            #{response.damages}, #{response.custody}, NOW()
        );
    </insert>

</mapper>
